# Auth Service

The auth service handles all authentication flows including:

- Login with email/password
- OAuth2 social login
- Token refresh
- Password reset

## API Endpoints

- POST /auth/login
- POST /auth/refresh
- POST /auth/logout
- POST /auth/reset-password
