#!/usr/bin/env bash
# Test stub for `supabase`. Logs argv to $INFRA_STUB_LOG and succeeds.
set -euo pipefail
if [ -n "${INFRA_STUB_LOG:-}" ]; then
  printf 'supabase %s\n' "$*" >>"$INFRA_STUB_LOG"
fi
case "${1:-}" in
  --version) printf '2.20.0\n' ;;
  *) : ;;
esac
exit 0
