name: Deploy
on:
  push:
    branches: [main]
jobs:
  quality-gate:
    runs-on: ubuntu-latest
    steps:
      - run: pnpm test
  deploy:
    needs: quality-gate
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploying without ever having tested api"
