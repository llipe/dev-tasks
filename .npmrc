# npm registry configuration for @llipe.com/dev-tasks
# Auth is handled by:
#   - CI: OIDC Trusted Publishers (no secrets needed)
#   - Local: `npm login` writes credentials to ~/.npmrc
registry=https://registry.npmjs.org/
