# npm registry configuration for @llipe/dev-tasks
# The auth token is provided via NPM_TOKEN environment variable (CI) or `npm login` (local).
registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
