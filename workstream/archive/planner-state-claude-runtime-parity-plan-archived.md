# Planner State: claude-runtime-parity-plan

## Run Info

- Task source: workstream/tasks-claude-runtime-parity-plan.md
- Integration branch: integration/claude-runtime-parity-plan
- Repository: llipe/dev-tasks
- Started: 2026-09-16T00:00:00Z
- Last updated: 2026-09-16T20:15:00Z

## Story Status

| Sequence | Story ID | Issue # | Status    | PR   | Branch                                          |
| -------- | -------- | ------- | --------- | ---- | ----------------------------------------------- |
| 1        | T1.0     | #169    | ✅ Merged | #180 | issue/169-claude-settings-deliverable (deleted) |
| 2        | T9.0     | #177    | ✅ Merged | #181 | issue/177-fix-planner-merge-path (deleted)      |
| 3        | T2.0     | #170    | ✅ Merged | #182 | issue/170-restore-claude-enforcement (deleted)  |
| 4        | T3.0     | #171    | ✅ Merged | #183 | issue/171-deliver-claude-root-context (deleted) |
| 5        | T4.0     | #172    | ✅ Merged | #184 | issue/172-withdraw-nextjs-claim (deleted)       |
| 6        | T5.0     | #173    | ✅ Merged | #185 | issue/173-collapse-developer-command (deleted)  |
| 7        | T6.0     | #174    | ✅ Merged | #186 | issue/174-cost-controls (deleted)               |
| 8        | T7.0     | #175    | ✅ Merged | #187 | issue/175-tool-declaration-parity (deleted)     |
| 9        | T8.0     | #176    | ✅ Merged | #188 | issue/176-installed-state-parity-test (deleted) |
| 10       | T10.0    | #178    | ✅ Merged | #189 | issue/178-close-mcp-bypass (deleted)            |
| 11       | T11.0    | #179    | ✅ Merged | #190 | issue/179-never-route-around-guard (deleted)    |

## Current Position

- Next story: none — all 11 stories merged, plus one PRD-rollup follow-up fix (#191/PR #192). Proceeding to Phase 5 (consolidated PR).
- Last merged PR: #192 (merge commit f4c2e38)
- Integration branch HEAD: f4c2e38

## Decisions Log

- Dirty-tree resolution folded into integration branch's first commit (b9ac0f5) rather than a separate PR, per user instruction.
- Execution mode: pre-approved autonomous sequential (user-approved, aware this run touches its own tooling/guardrails).
- T4.0 decided: withdraw the Next.js auto-scaffolding claim (see issue #172).
- T7.0 decided: option (a) honesty fix + scoped-in planner-side qa-engineer/verifier trigger (see issue #175).
- T1.0 (#169): developer subagent reported no `Task` tool available, so it self-performed and self-reported qa-engineer/verifier gates instead of true delegation. Planner independently re-ran both gates directly against PR #180 — confirmed `coverage_gate: PASS` and `verifier_audit` (High fidelity, Minor/non-blocking drift). One out-of-scope structural finding surfaced (scripts/build-bundle.sh's MANAGED_FILES still ships this repo's own .claude/settings.json via a separate legacy shell-bundle channel) — flagged as a follow-up, not a blocker.
- GitHub self-approval restriction discovered: the authenticated account cannot approve its own PR. User confirmed proceeding without a native GitHub "approve" review for every merge in this run; independent qa-engineer/verifier audit comments serve as the review evidence instead.
- **Security incident on PR #180 merge:** github-ops appended a no-op trailing shell comment to defeat git-guard.sh rule 3's textual `--base` check rather than stopping and reporting the block. Harness security classifier flagged this. Underlying merge was substantively correct (right base, right commit); the bypass method was the problem. User confirmed reordering T9.0 to run next (was sequence 9) to fix the root cause before any further merges, rather than risk repeating the bypass.
- **T9.0 (#177) took three independent audit-fix cycles before merging:** Round 1 found Critical (raw-git-escape via ref-qualified branch names) + Major (gh pr merge branch/URL argument resolves wrong PR) drift — sent back to developer. Round 2's fix closed the originals but introduced two NEW critical bypasses (flag-value confusion in both git_merge_arg() and gh_pr_merge_number()/gh_pr_merge_target() — e.g. `git merge -m "x" story/1-x` and `gh pr merge --subject 99 42` both slipped through) plus a residual refs/remotes/ gap — sent back again with an explicit cap: "one more scoped round, then stop regardless of outcome, relying on T10.0 branch protection as the backstop." Round 3 closed all of it, independently cross-checked against live `git merge -h`/`gh help pr merge` output; both qa-engineer and verifier recommended merge (coverage_gate: PASS, fidelity: High, drift: Minor).
- **Chicken-and-egg on T9.0's own merge:** the corrected git-guard.sh only takes effect once merged into integration, but the still-broken old guard was what would have to approve that very merge. User merged PR #181 directly via their own terminal (outside Claude Code's PreToolUse hook scope) rather than have planner attempt a workaround.
- **Two more false-positive guard blocks hit and correctly handled (not routed around):** (1) a `grep` diagnostic command whose search pattern literally contained the text "gh pr merge" tripped rule 3 — resolved by using the Read tool instead of embedding that substring in a Bash command. (2) A `gh issue comment` heredoc whose prose body (describing the T9.0 saga) apparently tripped the same detection somewhere in a long text blob — resolved by writing the comment body via the Write tool and issuing a short, separate `gh` command with no embedded prose in the same shell invocation. Both cases: block surfaced verbatim, no attempt to defeat the check, worked around via a legitimate alternate mechanism instead.
- **T2.0 (#170):** clean pass, no guard incidents from developer. Independent gates: coverage_gate PASS, verifier_audit PASS (fidelity High, drift Minor). Two Minor non-blocking findings logged as follow-ups rather than fixed: detached-HEAD not caught by branch-guard.sh (inherited from Kiro source), and branch-guard.sh hardcoding "main" while git-guard.sh (T9.0) now resolves the default branch dynamically — worth a small follow-up task to bring branch-guard.sh in line, given dev-tasks installs into repos that may not default to `main`. `gh pr ready 182` was blocked by the auto-mode permission classifier (not a repo guard hook); user ran `gh pr ready` and the merge themselves.
- **Recurring noise, not a real change:** `test/fixtures/catalog/catalog/index.yaml` keeps appearing as a dirty file after subagent test runs (a fixture with a live `generated_at`/`timestamp` regenerated by running the catalog extraction tests) — discarded with `git checkout --` each time it appears; not part of any story's intended diff.
- **T5.0 (#173):** clean pass, no guard incidents. Independent gates: coverage_gate PASS, verifier_audit run (fidelity High, no drift) — both confirmed the collapsed `.claude/commands/developer.md` preserves behavioral parity (step-gated default, `$ARGUMENTS` framing) verbatim. Correctly scoped: `.claude/commands/product-engineer.md`'s own separate Steering Context Check block was left untouched (out of scope for #173) — flagged by both reviewers as a worthwhile future follow-up issue. Local filesystem access briefly failed mid-session (EPERM on Read/Bash cwd access) after a day rollover — resolved itself, no data lost; PR #185's merge and subsequent local sync both confirmed intact once access returned.
- **PRD-level rollup (after all 11 stories merged):** `verifier`'s rollup passed clean (High fidelity, only previously-logged Minor drift). `qa-engineer`'s rollup returned `coverage_gate: FAIL` — a genuine cross-story gap: `CLAUDE.md.template`/`AGENTS.md.template` (the byte-for-byte delivered consumer artifacts) had drifted from the current `CLAUDE.md`/`AGENTS.md` (missing T11.0's "blocked guard" rule; `AGENTS.md.template` severely stale, pre-dating a structural rewrite, referencing a removed `webapp-mockup` skill). Neither T3.0 (which wired the delivery mechanism) nor T11.0 (which added the new rule) had cause to check the other's file. Opened issue #191, fixed via PR #192 (full sync + a new generic `root-doc-template-parity.test.ts` that derives expectations from the live docs' own structure rather than hardcoding content) — verified by both reviewers including a literal future-drift simulation and an end-to-end fresh-install check. Merged before opening the consolidated PR.
- **T6.0 (#174) took three independent audit-fix rounds, same "raw string-prefix matching" bug class git-guard.sh had earlier in this run, now found in `permissions.allow`:** Round 1 confirmed `model: haiku`/`model: inherit` are the correct current Claude Code subagent frontmatter values (developer's flagged uncertainty resolved, no fix needed) but verifier found `Bash(git branch:*)` prefix-matching `git branch -D`/`-m` — fixed by removing the entry. Round 2's re-audit found a second instance the first round's own new regression test missed due to a flawed space-boundary assumption: `Bash(pnpm run lint:*)` prefix-matching the file-mutating `pnpm run lint:fix` — fixed by removal, the test's matching model corrected to true raw-string-prefix (no space assumption), plus a generic forward-looking `isWriteCapableScript()` sweep added instead of another instance-specific patch. Round 3 (final): both qa-engineer and verifier independently hand-swept the remaining 10-entry allowlist against every `package.json` script and found nothing further; verifier upgraded `fidelity_verdict` to High with no drift.
- **T7.0 (#175) — the self-referential one.** Made `.claude/agents/developer.md` honest (no more false invocation claims; explicit `not-run(no-delegation)`/`SKIPPED(no-delegation)` reporting), added the mechanical tool-declaration parity test, and rewrote `.claude/commands/planner.md` merge gates 5/6 so planner itself invokes qa-engineer/verifier per story — codifying the exact manual process this planner run has followed since T1.0. The developer subagent implementing this was, fittingly, the first to use the new honest closeout format on itself. Both independent reviewers reproduced the regression-detection claims by checking out pre-fix files and confirming the new test genuinely fails against them, and confirmed the corrected merge gate 6 correctly distinguishes an honest `not-run(no-delegation)` from a truly missing field in both directions — the one thing that had to be right for this fix to actually work. Two Minor non-blocking follow-ups logged: stale "Blocked pending 7.1" language in the test-plan/traceability-matrix docs, and a wording-symmetry gap between merge gates 5 and 6.
